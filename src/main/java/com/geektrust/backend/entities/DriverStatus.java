package com.geektrust.backend.entities;
public enum DriverStatus {
    AVAILABLE, // The driver is available for rides
    BUSY,      // The driver is currently on a ride
    OFFLINE    // The driver is temporarily offline
}
